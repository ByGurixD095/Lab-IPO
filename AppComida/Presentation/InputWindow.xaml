<Window x:Class="AppComida.Presentation.InputWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Input" 
        SizeToContent="Height"
        Width="400"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="Transparent">

    <Border Background="{DynamicResource CardBackground}" 
            CornerRadius="12" 
            BorderBrush="{DynamicResource InputBackground}" 
            BorderThickness="1"
            Margin="10">
        <Border.Effect>
            <DropShadowEffect BlurRadius="15" ShadowDepth="5" Opacity="0.3" Color="Black"/>
        </Border.Effect>

        <Grid Margin="25">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock x:Name="TxtTitle" Text="Título" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource BrandColor}" Margin="0,0,0,15"/>

            <TextBlock x:Name="TxtMessage" Text="Mensaje de la pregunta..." TextWrapping="Wrap" Foreground="{DynamicResource TextPrimary}" FontSize="14" LineHeight="20" Margin="0,0,0,20" Grid.Row="1"/>

            <TextBox x:Name="TxtInput" Grid.Row="2" Style="{DynamicResource ModernInput}" Height="40" FontSize="16" FontWeight="SemiBold" VerticalContentAlignment="Center" Margin="0,0,0,25"/>

            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">

                <Button Click="BtnCancel_Click" Style="{DynamicResource BtnSecondaryStyle}" Width="110" Height="40" Margin="0,0,10,0" Padding="5" Cursor="Hand">
                    <Viewbox Stretch="Uniform">
                        <TextBlock Text="Cancelar" Margin="5,0"/>
                    </Viewbox>
                </Button>

                <Button Click="BtnOk_Click" Style="{DynamicResource BtnPrimaryStyle}" Width="110" Height="40" IsDefault="True" Padding="5" Cursor="Hand">
                    <Button.Effect>
                        <DropShadowEffect BlurRadius="5" ShadowDepth="2" Opacity="0.2"/>
                    </Button.Effect>
                    <Viewbox Stretch="Uniform">
                        <TextBlock Text="Aceptar" Margin="5,0"/>
                    </Viewbox>
                </Button>

            </StackPanel>
        </Grid>
    </Border>
</Window>